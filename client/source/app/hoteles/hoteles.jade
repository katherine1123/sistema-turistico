.dashboard-view.full-width
    .card.text-center(class="p+++")
        form.mt(name='signUpForm' novalidate sv-form-val)
            div(flex-container='row')
                div(flex-item='8')
                    lx-text-field(label='Ciudad de busqueda')
                        input(type='text'
                        name='busqueda'
                        ng-model='vm.busqueda'
                        mg-model-options = '{updateOn:"default "blur"}'
                        sv-unique-username
                        ng-minlength="2"
                        ng-maxlength="200"
                        required="error" )
                div(flex-item='4' class='p+')
                    a.btn.btn--green.btn--l.btn--raised(class="", ui-sref="root.login", lx-ripple="white") Get Started
    br                
    .card.text-center(class="p+++")
        div(ng-repeat="hotel in listaHoteles")
            .card
              div(flex-container='row')
                .card__img.card__img--left(style='height: 160px')
                    img(ng-src="{{hotel.imagen}}")
                div(flex-item='', flex-container='column')
                  div(flex-item='')
                    .div(class='p+')
                      strong.text-left.fs-headline.display-block {{hotel.nombre}}
                      span.text-left.fs-subhead.tc-black-2.display-block {{hotel.ubicacion}}
                      span.text-left.fs-subhead.color-azul.display-block Desde: {{hotel.precio}}
                  .card__actions.text-center
                    a.btn.btn--green.btn--l.btn--raised(ng-click="verHotel(hotel)") Ver detalles

